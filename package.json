{
  "name": "careos-local",
  "version": "1.0.0",
  "description": "Careos Localized MVP - Elderly Care Nurse Finder & Booking Platform",
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently --kill-others \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
